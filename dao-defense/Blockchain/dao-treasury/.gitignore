# Node / npm
src/node_modules/
dist/node_modules/
solver/node_modules/
verifier/node_modules/
npm-debug.log

# Organizer secrets — the production registry + flags hold real competition
# secrets, never commit them. Keep the .example files as the committed template.
verifier/teams.json
verifier/flags.json

# Python checker
checker/__pycache__/
*.pyc

# Generated at runtime — never commit team credentials, keys or deployments
src/runtime/

# Foundry build artifacts
dist/out/
dist/cache/
dist/broadcast/
solver/out/
solver/cache/
solver/broadcast/
solver/probe.txt
